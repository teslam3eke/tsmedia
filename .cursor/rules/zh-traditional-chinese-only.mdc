---
description: PWA／產品文案與可見程式字串只用繁體中文（zh-TW），禁用簡體字與大陸用語
alwaysApply: true
---

# 繁體中文 only（禁用簡體）

使用者要求 **PWA 與程式內可見的中文**一律符合 **繁體中文／台灣用語**。撰寫、修改或機器翻譯後貼上時，務必先改為 zh-TW 再提交。

## 必須

- **UI 字串**：`tsx`／`vue`／`html`／`manifest.json` 等對使用者顯示的文字只用繁體。
- **`lang`**：`index.html`、`manifest` 的 `lang` 維持 **`zh-TW`**（與專案約定一致）。
- **註解與可被協作者讀到的中文**：`src`、`public`、`supabase/migrations` 等處若以中文說明，同樣用繁體，避免混入簡體字造成搜尋與風格分裂。

## 字形／用語示例（曾因簡繁混用而入庫）

| ❌（易誤混入） | ✅（繁體／台灣慣例） |
| --- | --- |
| 站内、`内`（簡，`U+5185`）表示內部 | **站內**、**內**／**內部**（`U+5167` 起首之「內」） |
| 占位「占為簡寫」 | **佔位** |
| 场次 | **場次** |
| 留余／余量（剩餘義） | **留餘**／**餘量** |
| 全屏 | **全螢幕** |
| `默认`、`昵称`、`数据库` 等詞 | **預設**、**暱稱**、**資料庫** |

許多詞在 Unicode 上與繁體同形（如「並行」「為了」），僅確認語感為台灣慣用；重點是 **異體／簡化字別混進來**（如 **場、內簡形、占、餘／余、屏** 與對應用語）。

## 編輯時自檢

- 對改動段落掃過：是否出現 **簡化字或陸地用詞**。
- **機翻**：英文→中文後必做一次 **繁體化／在地化校對**。
- **`api/`、`scripts/`**：對外錯誤訊息或會進文件的 demo／文案比照 `src`。

## AI 與使用者對話

就此專案回覆維護者時：**請用繁體中文**書寫，與 PWA／產品用語一致。
